- new game state variable with cell config. This allows cell config to be altered on the fly so things like b and t cell specialisation can be stored on this global state variable, rather than duplicated across each individual deployed cell. Think about if anything else should be migrated to this global state variable.
- come up with a plan on how to introduce the metagame, i.e. a full meta run with life event choices between runs. Life event choices will have meaningful consequences (e.g. unlocking pathogen or cell types, or adding modifiers using existing modifier functionality). Events should be probabilistic, with certain events having eligibility criteria (e.g. what life stage, previous events have or haven't been chosen, certain conditions were met in previous games such as a certain node receiving a scar). I want a flexible framework that is easy to update and define events, effects and criteria (leveraging existing modifier framework where appropriate). There will be an extra meta layer to keep track of, i.e. the current overall meta-run, along with the sub-runs (main gameplay loops). In general, modifiers (scars/upgrades/life-events) will be persistent across the meta-run. Think about the UI, and how the player stays connected with the metarun. There should also be functionality for 'bonus objectives', which gives a bonus (i.e. upgrade) if an objective is met (e.g. no infection lasts longer than 2 turns at a certain node, or no dendritic cells was used etc)